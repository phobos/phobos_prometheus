source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in phobos_prometheus.gemspec
gemspec

gem 'phobos', git: 'git@github.com:klarna/phobos.git', ref: 'instrument-handlers'
